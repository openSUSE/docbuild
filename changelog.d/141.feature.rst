Automatically generates ``homepage.json`` from the Portal XML configuration during the ``docbuild metadata`` command, storing it directly into the JSON cache directory for frontend template consumption.